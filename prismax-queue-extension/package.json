{
  "name": "prismax-queue-notifier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && node scripts/build.js && npm run copy-assets",
    "buildOrig": "npm run clean && tsc && npm run copy-assets",
    "copy-assets": "copyfiles -u 1 src/manifest.json pages/**/* icons/**/* dist",
    "copy-icons": "copyfiles -u 1 icons/**/* dist/icons"

  },
  "dependencies": {
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.27.2",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}